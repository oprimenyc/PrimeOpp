# Migration

## 1.0.0 — Initial Production Build

Initial release. No prior versions to migrate from.

## Future migrations

When this package is integrated with PrimeOpp, Commerce Core, Foundry,
E.V.E., PrimeOS, Browser Operator, AMOS or VERIDIAN, refer to the
respective integration guide under `docs/`. Each integration is
opt-in via adapter registration; no breaking changes to this package's
public contracts are required.

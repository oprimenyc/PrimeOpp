{
  "name": "@workspace/commerce-worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --reporter=default",
    "worker:commerce-core:dry-run": "tsx src/worker.ts"
  },
  "dependencies": {
    "pg": "^8.20.0"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@types/pg": "^8.18.0",
    "tsx": "catalog:",
    "typescript": "~5.9.2",
    "vitest": "^2.1.0"
  }
}
